[project]
name = "analysis"
version = "0.0.1"
description = "PoC analytics for UC-XXXX-XXXX"
requires-python = ">=3.10"
dependencies = ["pandas>=2.2", "matplotlib>=3.9"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
